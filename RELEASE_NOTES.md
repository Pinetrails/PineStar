# StarNet v0.4.3

- Input isolation: removed all Win32 input-driver code paths from the sidecar — agents can no longer touch the real mouse/keyboard.
- Fixed agents escaping into the void: crew bodies now re-frame correctly across station origin shifts.
- Projects rail: file-explorer button with native folder picker; sessions organized under projects.
- Crew manifest UI redesign.
- Sharper StarNet logo assets.
- Approval prompts now route through the permission card instead of dead-end chat asks.
