StarNet desktop 0.1.8.

- Desktop auto-updater is now live via the public GitHub Releases channel (the old
  updates.starnet.app host never resolved, so prior installs could never update).
- Signed updater artifacts are produced again; installs verify each update's signature
  before applying it.

See INSTALL.md for first-time install instructions (unsigned installer / SmartScreen).
