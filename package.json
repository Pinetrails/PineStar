{
  "name": "skynet-harness",
  "version": "0.0.0",
  "private": true,
  "description": "Skynet — a real, gamified, downloadable AI-agent harness. Phase 0.0 scaffold: the v7 engine ported verbatim into frontend/, with the v7 headless tests running from test/.",
  "scripts": {
    "validate": "node test/validate_map.js",
    "test:world": "node test/test_world.js",
    "test:fast": "node test/schema.test.js && node test/contract.test.js && node test/clock-rng.test.js && node test/context.test.js && node test/lint-emits.js && node test/lint-determinism.js",
    "test": "node test/validate_map.js && node test/test_world.js && npm run test:fast",
    "serve": "npx -y http-server frontend -c-1 -p 8087"
  }
}
