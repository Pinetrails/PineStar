# Cron / Scheduled-Routines Integration Plan

> **StarNet** — a layered plan for making an agent **TRULY AUTONOMOUS**: it wakes on a schedule and does real, unattended work — a routine that fires `runAgentLoop` at a cron time with no browser, no Commander, no one watching. This is the companion to `HERMES_INTEGRATION_PLAN.md` and `MESSAGING_INTEGRATION_PLAN.md` and obeys the same discipline: one-line goals, Deliverables/Port-create/Tests/DoD per step, the Definition-of-Done, one-commit-sized steps, critical-path-first. Every seam below was confirmed against the real files; every premature/redundant/hallucinated claim flagged by the adversarial verdicts has been dropped or corrected.

> _Source: `NousResearch/hermes-agent` (MIT), cloned to `C:\Users\andro\hermes-ref`, cron subsystem (`cron/scheduler.py` + `cron/jobs.py` + `cron/blueprint_catalog.py` + `cron/suggestion_catalog.py` + `cron/suggestions.py` + `hermes_time.py` + the `hermes_cli/cron.py` CLI surface). **Pattern mine, not a port.** Produced by a parallel design + adversarial-verify pass against the real sidecar (2026-06-14). Prefer the dossier's `corrected` statements over any reader claim they contradict._

---

## 1. Preamble — what we are borrowing and the prime directive

Hermes fires scheduled agent runs through a **single-pass `tick()`** (`scheduler.py:1970`) that the gateway calls every 60s from a background thread (`scheduler.py:5`): it takes a file lock, computes due jobs via `jobs.get_due_jobs` (`jobs.py:1032`), advances each job's `next_run_at` **before** execution for at-most-once (`scheduler.py:2017-2018`), and runs each job **in-process** by constructing `AIAgent(...)` and dispatching `agent.run_conversation(prompt)` onto a per-job `ThreadPoolExecutor(max_workers=1)` worker with a contextvars-copied context + an inactivity-poll loop (`scheduler.py:1781-1786`) — in-process is the load-bearing fact (no subprocess, no HTTP, no IPC), but the call is wrapped in `concurrent.futures` threads, not run on the ticker thread. The schedule math is pure (`jobs.parse_schedule` `:206-292`, `jobs.compute_next_run` `:373-416`); the wall-clock timer lives in the gateway, not the scheduler. Unattended consent is a config-gated, default-deny posture under an unconditional hardline floor (`approval.py:1116-1155`). Job state persists to `~/.hermes/cron/jobs.json` with atomic temp+rename (`jobs.py:471-487`).

We are **mining this for patterns, not porting it.** The prime directive, in four clauses:

1. **It is a pattern mine, not a port.** We take the *shape* of an idea (a single-pass tick over pure due-job math; advance-before-run at-most-once; fast-forward-not-replay missed runs; a per-job in-flight lock; default-deny unattended consent under a hardline floor; an atomic versioned job store; a typed-slot blueprint catalog that emits the one real create-job; an anti-nag suggestion store) and re-derive the *smallest honest version* against our seams. We leave behind every Hermes-ecosystem artifact: the `ThreadPoolExecutor` parallel/sequential pool split, the `croniter` dependency, IANA `ZoneInfo` resolution + the env/config tz cascade + the cache, the `child_process` script-preprocessor + its path-jail + redaction + downgraded injection scanner, the `HERMES_CRON_SESSION`/`HERMES_CRON_AUTO_DELIVER_*` contextvar dance, the `_HOME_TARGET_ENV_VARS`/`_KNOWN_DELIVERY_PLATFORMS`/`Platform` enum delivery matrix, the 14-entry lifestyle blueprint catalog, the four-renderer (form/slash/deep-link/docs) sprawl, the cross-process `.tick.lock` (fcntl/msvcrt), and the threading/asyncio machinery.

2. **Node, not Python.** Our loop is single-threaded async with real I/O concurrency. There is no GIL, no `threading.Lock`, no `ThreadPoolExecutor`, no `as_completed` fan-out, no `asyncio.run` delivery coros, no fcntl/msvcrt file lock. A `setInterval` timer in the one ambient-I/O module + a `Set` of in-flight runIds + a `Map` is the whole concurrency mechanism. The Python "parallel pool vs sequential workdir pool" collapses to simple in-process serial dispatch through the one `runOnce` host.

3. **Nothing violates the existing discipline.** Every new module is UMD, zero-dependency, headless-testable with injected deps (an injected `clock` so no test ever reads the wall clock), deterministic (no bare `Math.random`/`Date.now`; `lint-determinism.js` scans `shared/` + `sidecar/`, excluding only `sidecar/index.js` — confirmed `lint-determinism.js:11,16,35`). **Every cross-boundary signal goes through `shared/events.js` + `shared/emitter.js` first — a new schema rung *before* any producer**, or `emitter.js` drops it (`emitter.js:17,20-23`) and `lint-emits.js` flags the literal `emit` (`lint-emits.js:37,45`). Every persisted shape is save-safe (versioned, atomic temp+rename, tolerate missing/corrupt → fail-closed). One step = one revertable commit, ≤3 files, gating test first, fast suite green (`< ~10s`) before the next.

4. **Nothing jumps the critical path.** Cron **is** roadmap step **1.7 triggers** (`HERMES_INTEGRATION_PLAN.md:19` names the Phase-1 spine `1.0 keychain → 1.5 consent → 1.6 PROPTERM → 1.7 triggers → 1.8 recovery/kill-switch`). The `'schedule'` trigger is **already frozen**: `shared/events.js:24-26` enumerates `agent.run.start.trigger ∈ ['directive','schedule','event']`, and `loop.js:115` already reads `const trigger = o.trigger || 'directive'` and emits it at `loop.js:125`. So a scheduled run is a *first-class, already-frozen* concept — the trigger contract predates its first scheduled producer. The genuinely-new signals are the *cron lifecycle* rungs (`cron.*`), which get their `shared/events.js` entry first. Cron rides the run host `runOnce` — **today still the monolithic inline core at `index.js:184-281`; `runOnce` does not exist yet** (grep: zero matches), so its extraction is a hard prerequisite performed by `MESSAGING_INTEGRATION_PLAN.md` Commit 4 **or**, if that hasn't shipped, by CRON **Commit 4a** (§5) — and the **existing** consent machinery (`surface:'autonomous'`, the fail-closed floor). It forks neither the loop nor the event contract.

---

## 2. Reconciliation — what already exists and must NOT be re-proposed

The following are **built and integration-tested today**; no part of this plan may re-introduce or re-fork them:

| Capability | Where | Note for the cron subsystem |
|---|---|---|
| The sole run-driving host: tools registered fresh → capability station + broker → provider+cost → dispatch → `runAgentLoop`, never throws | `index.js:184-290` | A scheduled fire is **just another caller** of this host. Do NOT propose a second run engine. |
| The `runOnce({key,model,system,messages,agentId,isTask,emit,signal,runId,surface,trigger,prompt})` extraction (factor the run core out of `handleRun`, called in-process not via `/api/run` loopback) | proposed `MESSAGING_INTEGRATION_PLAN.md §3.2, Commit 4` — **NOT built yet** (grep `runOnce`→0; `index.js:184-281` is still the inline core; MESSAGING is at Commit 1). CRON **Commit 4a** does the extraction if MESSAGING hasn't. | Cron is the **identical** non-browser caller shape as the messaging hub. Reuse the extraction; add `surface`+`trigger`+`runId`+optional `prompt` as params (B1/B2 below). Do NOT re-design it. |
| The frozen `agent.run.start.trigger` enum **already containing `'schedule'`**; `loop.js` already threads + emits `trigger` | `events.js:24-26`; `loop.js:115,125` | A fire passes `trigger:'schedule'` — **zero schema change** to the run/reply path. Do NOT add a trigger enum value, do NOT add a "run fired" rung for the run itself. |
| The autonomous consent surface: `surface:'autonomous'` → synchronous `{allow:false, reason:SILENCE}` on ungranted mutation, **before** the prompt branch; read-only/non-network auto-allow; hardline floor first | `permissions.js:41,84-85,91,92`; `index.js:209-210` comment anticipates it verbatim | This **is** the unattended-consent keystone (§3.5). Do NOT design a new "cron approval mode" — `surface:'autonomous'` + pre-blessed `permissions.allow.json` is the cleaner, default-deny-by-construction re-derivation. |
| The save-safe persistence idiom: versioned envelope `{version:1,...}`, atomic `FILE+'.'+pid+'.tmp'` → `writeFileSync` → `renameSync`, load → missing/corrupt → empty (fail-closed), persist **throws** on failure, stored as a **sibling of the fs jail** the agent's own `fs.*` tools cannot reach | `index.js:76-88` (allowlist), `:57-68` (notebook) | The cron-jobs store copies the **allowlist** variant (versioned, fail-closed, throws). Do NOT port Hermes's `strict=False` JSON-repair ladder or `threading.Lock`. |
| The channels delivery seam: `adapter.send(chatId,text,opts?)→{ok,messageId?,error?,retryable?}` + hub chunking + the `channel.delivery{channel,chatId,runId,ok,chunks,reason}` rung | `MESSAGING_INTEGRATION_PLAN.md §3.1,§3.3,§3.7` (**a MESSAGING-Commit-3 deliverable — NOT in `shared/events.js` today**; grep finds no `channel.*` rung, the file ends at the retired-cosmetics block `:79`) | A cron result-delivered IS `adapter.send` to a configured channel — **reuse**, do not build a second sender. Local/in-app delivery reuses the **already-present** `deliverable` (`fs.js:29`, `notebook.js:38`) + `notify` (`events.js:75`) rungs, so cron's local path is independently buildable without messaging. |
| Per-run caps that bind every caller: `CAPS={maxIters:16,maxCostUsd:1.00,maxRepeat:3,toolTimeoutMs,maxToolBytes}`, guarded pre-paid-call | `index.js:31,275`; `loop.js:130-131` | A scheduled run inherits these unchanged through `runOnce` — gen's per-run floor is **stronger** than Hermes cron (which has no USD cap). Do NOT re-propose per-run caps. |
| `index.js` is the **ONLY** ambient-I/O module (its header `:1-6` enumerates http/fs/fetch/`process.env`; `Date.now`/`crypto.randomUUID` are also used in-file, and it already owns `setTimeout` at `:178`/`:342` — **but no `setInterval` exists yet**, so the cron tick is net-new); the lint exempts the whole file | `index.js:1-6`; `lint-determinism.js:16` | The cron **timer + now-source + id minting** live in `index.js`; the cron-math module stays pure/injected-clock. This preserves the invariant rather than widening it. |

**What does NOT exist** (and is therefore genuinely new, requiring a rung-first or a new module):
- **No scheduler / tick / cron-math.** Grep confirms `sidecar/` holds only `context.js, cost.js, loop.js, permissions.js, index.js` — no `cron.js`, no `cron.jobs.json`, no tick source that fires a run without an HTTP request. Net-new.
- **No `cron.*` event rungs.** `shared/events.js` lines 22-80 contain none. The five lifecycle signals (§3.8) are proposed additions, schema-first.
- **No at-rest run config for a non-browser caller.** `handleRun` requires `key`+`model` in the POST body (`index.js:138-139`); a scheduled fire has no browser. The boot-frozen `SKYNET_OPENROUTER_KEY` / `SKYNET_DEFAULT_MODEL` reads **do not exist yet** (only `SKYNET_PORT`@`:28` and `SKYNET_FULL_ACCESS`@`:73` are read) — they are net-new beside line 73, shared with the messaging plan's §3.5.
- **No cumulative/daily spend ledger.** Every cost ceiling is per-run (`index.js:31`, `loop.js:131`); `spentUsd` resets each run. There is no process-wide/per-day accumulator anywhere in `sidecar/`. This is a real cron-specific gap (§3.7 flag).
- **No ROUTINES panel, no `/api/cron/*` endpoints.** `stationui.js` has five panels (AGENTS/SKILLS/TASKS/SETTINGS/NOTIFS); none is cron. Net-new.
- **No schedule-preview / "next N fire times" UX anywhere.** Confirmed absent across the Hermes cron path (repo-wide grep: no `--dry-run`, no preview, no multi-`get_next` loop). This is net-new GUI value gen *adds*, not ports.

---

## 3. The design

### 3.0 Shape, end to end

```
sidecar/index.js  ── setInterval(60s) ──► tick(now = Date.now())          [the ONE ambient timer + now-source]
   (composition root, lint-exempt)            │
                                              ▼
                              cron-math (sidecar/cron.js — PURE, injected clock)
                                 • planTick(jobs, now) -> { fire[], skipped[{jobId,reason}], next[{jobId,nextAt}] }
                                 • dueJobs / nextFireAt / parseSchedule    (no Date.now, no setTimeout, no rng)
                                              │  returns DATA (which jobs fire + the advanced next-fire to persist)
                                              ▼
                              index.js applies the plan (the side effects):
                                 • advance nextRunAt BEFORE run (at-most-once), persist via atomic temp+rename
                                 • check the in-flight Set (one-run-per-job lock); emit cron.skipped if locked
                                 • for each fired job: mint runId (crypto.randomUUID), build runSpec from the stored job
                                              │  { key (boot-frozen), model, system, messages, agentId, isTask, surface:'autonomous', trigger:'schedule' }
                                              ▼
                              runOnce(...)  (index.js — the SAME run host the browser uses)
                                 • runAgentLoop → frozen agent.* events on an in-process emit sink
                                 • surface:'autonomous' ⇒ ungranted mutation default-denies (permissions.js:92)
                                              │  buffer agent.token by runId; watch agent.run.end / .error
                                              ▼
                              DELIVERY (reuse the channels seam + deliverable + notify):
                                 • [SILENT] strict-equals check on the buffered reply  ── suppresses delivery, keeps audit
                                 • deliver=channel → adapter.send (chunked) + channel.delivery rung
                                 • deliver=local   → fs.*/notebook.write deliverable file (agent's own tools)
                                 • in-app cue     → notify rung; cron.result {jobId,runId,outcome}
```

The cron-math module never imports the loop, the provider, the broker, or `fs`. The **`index.js` tick driver** is the analogue of Hermes's gateway 60s thread + `tick()` executor: the one seam where wall-clock time drives the pure plan, which drives the existing run host.

### 3.1 The PURE cron-math module vs the index.js-owned TIMER + now (the determinism split — load-bearing)

This split is the spine of the whole subsystem and is dictated by `lint-determinism.js`. The lint's exact scan set is `ROOTS = ['shared','sidecar']` (`lint-determinism.js:11`), banning `/\bMath\.random\b/`, `/\bDate\.now\b/`, `/\bperformance\.now\b/`, `/\bnew\s+Date\s*\(\s*\)/` (`:35`), excluding **only** `sidecar/index.js` (`:16,45`). So the scheduler must be cut along exactly that line — mirroring how `loop.js` takes an injected `clock` (`loop.js:84,89`) and `index.js` wires `clock:{now:()=>Date.now()}` (`index.js:276`).

**Lives in `sidecar/index.js` ONLY (the ambient half — exempt by `:16`):**
- The **now-source** — the real `() => Date.now()` (the same shape already injected at `index.js:276`).
- The **timer** — a single `setInterval` (or self-rescheduling `setTimeout`) at the tick cadence. `index.js` already owns timers (consent timeout `:178`, search throttle `:342`); the cron timer joins them. The tick body is a thin `() => applyTick(Date.now())` so the only untestable line is the timer registration itself.
- **Id minting** — `crypto.randomUUID()` for each fire's `runId` (the same call site shape as `index.js:148`); `lint-determinism` forbids bare `Math.random` outside `index.js`, so ids are minted here and injected, never inside the pure module.
- **Persistence** — the load/persist of `cron.jobs.json` (atomic temp+rename), the only `fs` in the subsystem.

> **Cadence flag:** Hermes's 60s tick is from the `scheduler.py:5` docstring, not verified against the gateway ticker. gen picks its own cadence (60s proposed); the cadence is an `index.js` constant, not a contract.

**Becomes a PURE, lint-clean, injected-clock module `sidecar/cron.js`** (headless-testable like `loop.js`/`permissions.js`, zero ambient time). Proposed surface, mirroring `jobs.parse_schedule`/`compute_next_run`/`get_due_jobs`:

```
parseSchedule(str, now)              -> { kind:'once'|'interval'|'cron', ... }   // once needs `now` ONLY to resolve a duration→runAt; `now` is a PARAM, never Date.now()
nextFireAt(schedule, lastRunIso, now)-> int ms        // the SINGLE next fire strictly after the anchor (last run, else now)
dueJobs(jobs, now)                   -> { fire:Job[], advanced:[{jobId,nextAt}] }  // fire set + the advanced next-fire to persist (the no-backlog pair, see §3.3)
planTick(jobs, now)                  -> { fire[], skipped[{jobId,reason}], next[{jobId,nextAt}] }   // the whole tick as DATA
```

- **The `parseSchedule` four-branch shape** is the cheap win (mirror `jobs.parse_schedule:206-292`): `every Xm/Xh/Xd` → `{kind:'interval',minutes}`; bare duration `30m`/`2h` → `{kind:'once',runAt:now+Δ}`; ISO timestamp → `{kind:'once',runAt}`; 5-field cron → `{kind:'cron',expr}`. **`croniter` is a hard dependency gen cannot take** (zero-dep). The honest minimal cut: ship `interval` + `once` first (covers "every 1h"/"every 30m"/"in 2h"/timestamp — the bulk of real routines), and treat 5-field cron as a **deferred hand-rolled subset** (`*`, ranges, lists, steps) — explicitly **NOT** a port of croniter's full grammar.
- **The determinism implication is total:** because `now` is a parameter, "what fires next at 09:00" / "is this job due?" / "advance-before-run at-most-once" are all testable by feeding a fixed `now` and a fake clock; `croniter`/`ZoneInfo` would pull in ambient OS state and is the opposite of that.
- **tz/DST trap (flag in the module header):** the lint bans only the **zero-arg** `new Date()` (`:35`); `new Date(ms)` with an argument is lint-clean. If any tz math constructs a date from a passed-in `ms`, that is permitted — but a careless future "simplify to `new Date()`" regresses determinism. **For v1, server-local time (a single `clock.now()`) is the honest default** — do NOT port Hermes's IANA `ZoneInfo` resolution, env/config cascade, or cache (`hermes_time.py:37-88`). Keep the *seam* (next-fire computed against an injected `now`) so a tz layer can slot in later without touching cron-math.

> **DST scope is explicit (the silent landmine the interval-only v1 sidesteps).** v1 ships **`interval` + `once` only**, which are **duration-anchored** (the period runs from the last anchor, not a wall-clock time-of-day), so wall-clock **DST transitions do NOT affect them** — a DST gap/overlap shifts an interval job by at most the offset once, self-correcting on the next anchor. **Time-of-day / 5-field cron scheduling is deferred** (above) — and that is *exactly* where DST correctness becomes load-bearing: when that subset lands it MUST resolve fire times against a tz-aware `now` (the kept seam), and it carries its **own DST test** (spring-forward skips the missing hour; fall-back does not double-fire). So v1 has no wall-clock time-of-day scheduling and therefore no DST hole; the deferral consciously carries the tz obligation (see §6 Open Decision #1).

**Test pattern (already established twice in the suite):** inject `makeClock(T0)` from `shared/clock-rng.js` (documented `:6`, defined `:23` — `{now(),advance(ms),set(ms)}`), `clock.advance(ms)`, and assert `cron.js`'s pure functions fire exactly the expected jobs; assert `JSON.stringify(planA) === JSON.stringify(planB)` for byte-identical determinism (the exact shape `loop.replay.test.js:76` uses). Inject a fake `emit` (capture into a `seq` array), a fake `launchRun` spy, a fake `deliver` spy — the DI-purity precedent `permissions.test.js:116,122-125` sets.

### 3.2 The CronJob record + save-safe store

The persisted envelope is **`{ version: 1, jobs: [CronJob...] }`** — copying the **allowlist** idiom (`index.js:76-88`): atomic `FILE+'.'+pid+'.tmp'` → `writeFileSync` → `renameSync`; load → missing/corrupt → `{version:1,jobs:[]}` (fail-closed); persist **throws** on failure so a failed create/edit surfaces rather than silently "scheduling." Location: **`WORKSPACES/cron.jobs.json`**, a sibling of `permissions.allow.json` and the per-agent notebook — **outside** every `WORKSPACES/<aid>/` fs jail, so the agent's own `fs.*` tools can neither read nor rewrite its own schedule. (Hermes stores `~/.hermes/cron/jobs.json` with the same atomic temp+rename + `{"jobs":[...]}` wrapper, `jobs.py:471-487`, but **has no `version` field** — the `{version:1}` envelope is a gen addition matching the allowlist, not a port.)

The smallest honest `CronJob` shape (re-derived from the canonical `jobs.create_job:638-671`):

```
CronJob = {
  id:            string,        // minted in index.js (crypto.randomUUID), immutable, a single safe path component
  name:          string,
  prompt:        string,        // "" allowed
  schedule:      { kind:'once'|'interval'|'cron', ... },   // once:{runAt}, interval:{minutes}, cron:{expr}
  scheduleDisplay: string,      // human label, from parseSchedule
  agentId:       string,        // which agent runs (validated /^[A-Za-z0-9_-]{1,40}$/, reusing index.js:54)
  model:         string|null,   // per-job override (gen already supports per-run model); null → boot-frozen default
  deliver:       string,        // 'local' default; 'channel:<chatId>' once messaging lands
  enabled:       boolean,
  state:         'scheduled'|'paused'|'completed'|'error',
  repeat:        { times: number|null, completed: number },   // times=null = forever; one-shot auto times=1
  createdAt:     string,        // ISO, minted in index.js
  nextRunAt:     string|null,
  lastRunAt:     string|null,
  lastRunId:     string|null,
  lastStatus:    'ok'|'error'|null,
  lastError:     string|null,
  lastReason:    string|null,   // last agent.run.end reason (done|max_iters|budget|cancelled|error|refusal) — reuse the frozen enum
  // record-the-field, defer-the-consumer (Hermes fields with NO gen runtime consumer yet — store faithfully, no migration later):
  skills:        string[],      // gen has no skill store (HERMES L8 → RPG-2); store, do not consume
  script:        string|null,   // no script-runner host (§4); store, do not consume
  workdir:       string|null,   // no workdir injection; store, do not consume
  contextFrom:   string[]|null, // no job-chaining/output-store; store, do not consume
}
```

**Dropped vs recorded (justified):**
- **Drop now:** `skill` (legacy single-skill mirror — gen has no legacy data), `provider`/`base_url` (single-provider OpenRouter; HERMES L5 defers ProviderProfile), `no_agent`/`enabled_toolsets`, `paused_at`/`paused_reason` (fold into one optional field only if a consumer needs it), `origin`/`last_delivery_error` (delivery-origin is a channels concern — add with the messaging integration), `updated_at` (replaced by the `version` envelope), and the `threading.Lock` (no Node analogue — single-threaded async; prime directive clause 2).
- **Record the field, defer the consumer:** `skills`, `script`, `workdir`, `contextFrom` — no gen runtime consumer today; store them so a later commit wires the consumer with no migration, exactly the HERMES L5/L8 rule ("land it the commit before the consumer exists").
- **The `id` path-component guard is real and worth keeping:** the id is used as a path component for any output dir; validate it is a single safe component (the fs jail already rejects `..`/absolute/drive, `fs.js:47`) before any output-path use.

**Lifecycle ops** (pure reducer over the job array — no clock, no fs, no rng; timestamps/ids injected): `create`, `update` (rejects immutable `id`; recomputes `nextRunAt` when schedule changes unless paused), `pause`/`resume` (= enable/disable; resume recomputes `nextRunAt` from now), `markRun` (writes `lastRunAt`/`lastStatus`/`lastError`/`lastReason`, increments `repeat.completed`, deletes the job on repeat-limit exhaustion), `remove`. The reducer lives in a clock-free module; the wall-clock/id/fs all live in `index.js`.

### 3.3 The scheduler tick + missed-run/catch-up + overlap/concurrency + crash recovery

**The tick is a single pass, not a loop** (mirror `scheduler.py:1970`): on each `setInterval` wake, `index.js` reads `now = Date.now()`, calls the pure `planTick(jobs, now)`, applies the returned plan (persist advances, launch fires, emit `cron.*`), and returns. The wall-clock repetition is the `setInterval`, not the math.

**Missed-run / catch-up policy — fast-forward / skip, NOT run-each-missed** (the subtlest area; mirror `jobs._get_due_jobs_locked:1032-1062`): for a recurring job whose `nextRunAt <= now`, compute a **grace window** (Hermes: half the period, clamped 120s–7200s, `_compute_grace_seconds:341-370`). If late by **more than grace**, declare it a *stale missed run*: fast-forward `nextRunAt` to the next future occurrence and **skip** (emit `cron.skipped{reason:'caught-up'}`). Within grace, fire **once**. So a host down for hours fires **at most one** catch-up per recurring job, never a backlog burst. **For interval jobs the grace `period/2` is trivial and dependency-free**; the croniter-based grace period-measurement (`jobs.py:358-366` constructs croniter twice) is accretion deferred with the cron-expr subset. One-shots get a fixed small grace and are **permanently ineligible once `lastRunAt` is set** (`jobs.py:328-329`).

> **`cron.skipped` reasons are net-new gen signal, not a mined Hermes enum (flag).** Hermes emits **no** structured skip event — it only logs *lines*: `"Job %s already running — skipping"` (`scheduler.py:2111`) and `"Fast-forwarding to next run"` (`jobs.py:1046-1053`). gen re-derives the *math* of `_get_due_jobs_locked` but the `cron.skipped{reason ∈ ['already-running','disabled','caught-up','no-capability','stale-lock-reclaimed']}` rung is a fresh structured signal gen adds (§3.8), not an enum ported from Hermes. "Mirror `jobs._get_due_jobs_locked`" means *re-derive the fast-forward/skip decision*, not *port an enum that doesn't exist*.

> **The no-backlog guarantee is a PAIR (flag).** `nextFireAt` advances by a *single* period from the anchor; the fast-forward to "next future" happens in `dueJobs`, not in `nextFireAt`. If you re-derive `nextFireAt` alone without the `dueJobs` fast-forward, you lose the no-backlog guarantee — they must ship together (`jobs.py:389` vs `:1044`).

**Overlap / concurrency — one-run-per-job (SKIP, at-most-once-in-flight) with a SELF-HEALING lease:** Hermes keeps a process-global `_running_job_ids` set + `_submit_with_guard` refuses to dispatch a job already running (`scheduler.py:166-167,2110-2112`), and even its `finally` (`scheduler.py:2119-2121`) does **not** cover a hung worker. gen's analogue must be a *self-healing lock, not a bare `Set` add/clear* — a zombie run that never emits `agent.run.end` must never permanently wedge its job out of the schedule:
  - **(a) Clear on EVERY terminal path, in a `finally`.** The in-flight entry is released not only on the happy `agent.run.end`, but on `agent.run.error`, on the run's `AbortController` abort, AND on any uncaught throw in the reply-assembler — never only on "completion."
  - **(b) A lock LEASE, keyed by start time.** Store a **`Map<jobId, startedAtMs>`** (not a `Set`) in `index.js`. At the top of each tick, evict any entry older than a max-run ceiling (a new `CRON_MAX_RUN_MS`, default the `maxIters × toolTimeoutMs` ≈ 8-min worst-case bound from above) **and `abort()` that run's `AbortController`**, emitting `cron.skipped{reason:'stale-lock-reclaimed'}` (the new enum value added to the `cron.skipped` rung in §3.8). This guarantees a hung/zombie run is reclaimed within one lease window: the next tick aborts it and the job becomes eligible again.

A normal in-flight job is checked before dispatch (emit `cron.skipped{reason:'already-running'}` if locked). Combined with **advance-before-run** (the advanced `nextRunAt` persisted *before* launch), a long job that overruns its next tick is simply not re-dispatched; the next tick after it completes (or after the lease reclaims it) finds it due naturally. **Across different jobs:** gen dispatches serially in-process through the one `runOnce` host — the Python parallel/sequential pool split and `TERMINAL_CWD` env mutation are accretion (`scheduler.py:2095-2154`), left in the mine. The cross-process `.tick.lock` (fcntl/msvcrt, `scheduler.py:231-235`) is unnecessary — gen is a single in-process host with no second ticker process.

**Crash recovery — TWO halves, both required.** The plan must cover both *don't double-fire* AND *do resume what was missed while down*:

1. **At-most-once across restart (don't double-fire):** persist the advanced `nextRunAt` **before** launching the run (the advance is a returned value from the pure plan, applied by the host). If the process crashes mid-run, the bumped `nextRunAt` means the job won't re-fire on restart; `markRun` later overwrites `nextRunAt` from `lastRunAt` on completion (`jobs.py:902,939-965`).

2. **BOOT / RESUME reconcile (actually resume after restart):** at startup, when the env gate is on, `index.js` **loads `cron.jobs.json` and runs ONE immediate `applyTick(Date.now())` BEFORE registering the `setInterval`.** This is what makes a restart *resume* jobs rather than merely not-double-fire: the pure dueJobs fast-forward/catch-up math (§3.3 missed-run policy) is applied to runs that elapsed while the host was down, so a recurring job whose `nextRunAt` passed during downtime gets **exactly one** catch-up fire within grace (else fast-forward+skip) — never a backlog. State it plainly: *a restart re-arms the timer **and** runs an initial reconcile tick; the catch-up policy of §3.3 (fire one within grace, else fast-forward+skip) is what makes resume safe.* Without this step the "resume jobs after restart" goal is unmet — the timer would only arm for *future* fires and silently drop everything missed during downtime.

A Commit-4 test asserts both: (1) at-most-once — call pure `planTick`, persist, "restart" with the same fake clock, assert no double-fire; (2) resume — persist a recurring job with `nextRunAt` in the past, simulate restart with a fake clock advanced past one grace window, assert **exactly one** catch-up fire (not a backlog) and that the timer is then armed.

**A throwing job does NOT kill the scheduler:** wrap each fired run in a try/catch that records `lastStatus:'error'`+`lastError` and continues the dispatch loop — pure bookkeeping on the job record (the existing `agent.run.error{transient}` rung + the L3 classifier handle the run-level error vocabulary; no Hermes-style three-layer nesting). The "never silently disable a recurring job" invariant is worth keeping: a recurring job that can't compute a next run goes `state:'error'` but stays `enabled` (`jobs.py:910-929`); only one-shots flip to `enabled:false, state:'completed'`.

**What actually bounds a hung/idle run (the hang-guard — name it explicitly):** `maxIters:16` + `maxCostUsd:1.00` (`index.js:31`) alone do **NOT** bound a run that hangs with no token activity — neither cap advances while a tool never returns, and `loop.js` has no wall-clock/idle deadline (only `signal.aborted` at `:129,145,162`). The real bound is **`CAPS.toolTimeoutMs:30000`** (the per-tool timeout enforced at `sidecar/tools/registry.js:33`): every tool call either returns or times out in ≤30s, so the worst-case run duration is `maxIters × toolTimeoutMs` ≈ **8 min**. Without naming `toolTimeoutMs` as the guard, "per-run caps cover the runaway/hang case" is incomplete: a tool that never returns would otherwise hold the in-flight lock forever and wedge that job (see the §3.3 self-healing lock below). gen has **no** run-level wall-clock/idle kill of Hermes's kind — that is a deliberate gap raised in §6 Open Decision #10. **Left in the mine:** the inactivity-timeout poller + activity tracker + `agent.interrupt` (`scheduler.py:1759-1845`, default `HERMES_CRON_TIMEOUT` 600s), the prompt-injection scanner, MCP-orphan reaping, credential-pool/provider-fallback wiring.

**Transient-failure retry/backoff (the failure path beyond isolation).** Isolation (above) keeps one error from killing the scheduler, but it is *not* the whole failure story: a run that ends `reason:'error'` with **`transient:true`** (network/429/5xx — the honest signal the L3 classifier already emits at `loop.js:157`, currently unused by any cron consumer) deserves a bounded retry, not a full-period wait. **Policy:** on a fired run ending `transient:true`, set `nextRunAt = now + smallBackoff` (e.g. 60–120s, not a full period), store `retryCount` on the `CronJob`, cap at **N attempts per scheduled occurrence**, and reset `retryCount` on the next clean fire. A **permanent** error (`transient:false`) records `lastError` and waits the normal period — no retry storm. This is a small pure addition to the `markRun` reducer (it already owns `lastStatus`/`lastError`); the backoff time is computed in `index.js` from the injected `now`. **If retry is out of scope for v1, it is deferred-with-a-note:** see §6 Open Decision #11 — it must not be silently absent.

### 3.4 The fire → runOnce bridge (in-process emit sink, surface:'autonomous', trigger:'schedule')

A fired cron job is a clean `runOnce` caller. The bridge is **in-process, not a `/api/run` HTTP loopback** — matching Hermes's own in-process `agent.run_conversation` (`scheduler.py:1786`) and the messaging decision (`MESSAGING_INTEGRATION_PLAN.md:106-109`). A loopback would re-serialize the body and lose the in-process `emit`.

**The in-process emit sink** (structurally identical to the messaging hub `§3.3`): `index.js` builds an `emit` still wrapped by `makeEmitter` (so scheduled runs get the same frozen-event validation + redaction the browser gets) over an in-process `bus.emit`, not `res.write`. The sink: on `agent.run.start` capture `runId` (`loop.js:125`); on `agent.token` `buf[runId] += payload.delta` (the canonical reassembly `harness.js:91`; there is **no** `agent.message`/`agent.reply` event — `agent.token` is the only honest reply path); on `agent.run.error` `errMsg = payload.message`; on `capdenied` `errMsg = 'no '+need+' — '+reason`; on `agent.run.end` the turn is over — the buffered text is the result, `reason` says whether it completed (`done`) or truncated (`max_iters`/`budget`/`cancelled`).

**The exact runSpec a fire builds** (the fields `index.js:138` destructures, sourced for a no-browser caller):
- `key` — **boot-frozen `process.env.SKYNET_OPENROUTER_KEY`** (net-new, beside `index.js:73`; shared with messaging §3.5). Threaded into `runOnce`'s provider+web-tools exactly as the browser's `key` (`index.js:187,216`).
- `model` — the stored job's `model`, falling back to **boot-frozen `process.env.SKYNET_DEFAULT_MODEL`**.
- `system` — the job's persona (a fixed config persona, optionally per-agent).
- `messages` — the job's `prompt` as the user turn (cron is a fresh task per fire; no live transcript — Hermes also clears session vars, `scheduler.py:1524-1528`).
- `agentId` — the job's `agentId` (e.g. `cron_<jobId>` namespace so its notebook/workspace/allowlist don't collide), validated `/^[A-Za-z0-9_-]{1,40}$/`.
- `isTask: true` — a scheduled run is a task; the `supportsTools` pre-check (`index.js:221`) still applies.
- **`surface: 'autonomous'`** and **`trigger: 'schedule'`** — the two params `runOnce` must accept (today both hardcoded: `surface:'interactive'`@`index.js:211`, `trigger:'directive'`@`:222,277`).

The capability projection is **identical** to a directive run — no special-casing: `index.js:195-201` builds the station and calls `resolveTools(agentId, station)` (a pure projection that reads no clock/env/trigger, `resolve.js:21-46`); object placement = the run's authority ceiling, scheduled or not. A cron agent whose room has no `cabinet` cannot touch files — same rule, same code path; removal-revokes-next-turn holds for cron because tools re-resolve per run.

> **B1/B2 (signature corrections, flagged):** the extracted `runOnce` body also closes over `runId` (created at `index.js:148`) and the consent `prompt` channel (`promptConsent`, `:163-181`). The clean fix: add `runId` and an optional `prompt` to the signature. A cron caller mints its own `runId`+`ac` (`AbortController`) and passes `prompt: undefined` (no human to ask) and `surface:'autonomous'`. Leaving a cron run at `'interactive'` with no answerer would hang every ungranted mutation until the 120s auto-deny (`index.js:178`) — a correctness/latency bug (it still fails CLOSED, just slowly); `'autonomous'` returns the synchronous deny at `permissions.js:92` **before** the prompt branch (`:96`).
>
> **The safety property depends entirely on Commit 4a.** The whole unattended-consent guarantee of §3.5 holds *only if the cron caller actually passes `surface:'autonomous'`* — which in turn requires Commit 4a to have added the `surface` param to `runOnce`. If 4a is skipped or deferred, a cron fire inherits the hardcoded `surface:'interactive'` (`index.js:211`) + `promptConsent`, and every ungranted mutation stalls to the 120s auto-deny instead of denying synchronously. So **passing `surface:'autonomous'` is a Commit-4 DoD assertion, not just prose** (the regression lock: `harness.integration.test.js:132-140` already proves the autonomous default-deny — the cron tick test must assert the fire passes `'autonomous'`).

### 3.5 UNATTENDED CONSENT (default-deny + pre-blessed allowlist + hardline floor) — the safety keystone

A cron run has **no human at the keyboard**. The broker already has the exact rung for this; the entire story is **pass `surface:'autonomous'`**. The ladder walked for a scheduled run (`permissions.js:81-99`):

1. **HARDLINE (tier 1, `:84-85`)** — `hardlineFloor` (`index.js:102-107`) runs **first**, before any flag. A write to `.env`/`.git` is denied with the anti-retry suffix regardless of bypass. The floor stands for cron exactly as for directive runs — the gen analogue of Hermes's unconditional `HARDLINE_PATTERNS` floor (`approval.py:260-282,1116-1124`).
2. **BYPASS (tier 2, `:86-87`)** — only if frozen `FULL_ACCESS` (`index.js:73`, the gen analogue of Hermes's `_YOLO_MODE_FROZEN` frozen at import to defeat prompt-injection escalation). If the operator set machine-wide `SKYNET_FULL_ACCESS`, cron inherits it — the operator's explicit, boot-frozen decision; still below the hardline floor.
3. **CACHE (tier 3, `:88-89`, via `granted()` `:74-79`)** — **the pre-bless channel for cron.** `grantsPermanent = loadAllowlist()` from `WORKSPACES/permissions.allow.json` (`index.js:76-88`), a Set of `capability:scope` danger keys. To let a scheduled job write files unattended, the user pre-blesses e.g. `fs.write:write` / `notebook.write:write` **once, interactively, in a normal directive run** (the `always` decision persists it). Every future cron run reads it back here and auto-allows that class — no human needed. The allowlist lives **beside** the fs jail (sibling), so the agent's own `fs.*` tools can neither read nor self-edit it — the gen analogue of why Hermes freezes YOLO and gates writes to `~/.hermes/config.yaml`.
4. **RESOLVE (tier 4, `:90-98`)** — `surface` decides:
   - read-only, non-network → **auto-allow** (`:91`): a cron job that only reads files / queries the notebook needs no human and no pre-bless.
   - **`surface === 'autonomous'` → DEFAULT-DENY** (`:92`): `return { allow:false, reason: SILENCE }` synchronously, `SILENCE = 'autonomous run cannot self-approve this action — silence is not consent'` (`:41`). `consent.grant` is **never** called automatically (`:101-102`). The interactive `prompt` branch (`:96`) is **unreachable** for cron (`surface !== 'interactive'`), so the run never await-pauses on a prompt no one answers.

**Net behaviour for an unattended run:** read-only/non-network → silently works; pre-blessed danger class → silently works; everything else mutating → default-denied with `SILENCE`, producing the paired isError 'denied' result (`sidecar/tools/registry.js:79`) so the run continues honestly and finishes/reports rather than hanging.

This is **strictly more principled than Hermes's posture.** Hermes's cron default-deny is *narrower than it sounds*: it applies **only to the dangerous-pattern subset of shell commands** (`detect_dangerous_command`). A command that is **not** matched as dangerous is **auto-approved in cron context regardless of `cron_mode`** — the non-interactive non-gateway fall-through returns `{"approved":True}` at `approval.py:1131-1133` (non-dangerous) and `:1156-1161` (even dangerous, when not `HERMES_CRON_SESSION`+`cron_mode:deny`), with a logged warning (mirror at `:1379-1396`). So Hermes cron is *pattern-based default-deny on the dangerous subset of shell commands* via `approvals.cron_mode: deny` (`approval.py:1143-1153`), and the single config key `cron_mode: approve` removes even that, flipping the dangerous subset to **auto-approve everything-not-hardline**. gen's posture is **capability-scope-based** and default-deny **by construction** (`surface:'autonomous'`) across *every* ungranted mutating capability — not a pattern list — with **no global "approve everything" switch**, only per-danger-class pre-blessing. One-line verdict: gen avoids Hermes's "flip one config key to auto-approve everything" opt-out by making consent a per-danger-class capability grant rather than a pattern-list bypass that only ever covered the dangerous-command subset in the first place.

### 3.6 DELIVERY (reuse the channels seam + local deliverable file + in-app notification; the [SILENT] no-spam check)

A fire completes with no browser attached. Three honest sinks, **additive** — a fire may write a file, queue an in-app cue, and (if configured) push to a channel:

- **(a) Messaging CHANNEL — reuse `adapter.send`, do not rebuild.** Hermes resolves `deliver` against a `_HOME_TARGET_ENV_VARS`/`_KNOWN_DELIVERY_PLATFORMS` matrix with a live/standalone dual-send (`scheduler.py:659-851`, `116-142`). gen's analogue is the messaging plan's seam: `adapter.send(chatId,text)` (the structural match to Hermes's `runtime_adapter.send(chat_id,text,metadata)`) + the hub's chunk-to-`MAX_MESSAGE_LENGTH` + the **`channel.delivery{channel,chatId,runId,ok,chunks,reason}`** rung (`MESSAGING §3.1,§3.3,§3.7` — **frozen by MESSAGING Commit 3, not yet in `shared/events.js`**; an implementer must not expect to find a `channel.*` rung in the file today). A cron delivery is the identical "final assistant text → chunk → `adapter.send`" path; the only difference is the trigger (`schedule`) and the target (a configured channel record vs an inbound chat). **Dependency:** sink (a) requires the messaging adapter/hub to exist (MESSAGING commits 1-5, unbuilt). Until then, (b)+(c) are the available sinks. **Do NOT port** `Platform` enum / dual-send / `PlatformRegistry`.
- **(b) Local DELIVERABLE FILE — already works end-to-end, zero new write code.** The agent's `fs.write`/`fs.append`/`notebook.write` tools already emit the validated `deliverable` event from the producer (`fs.js:29`, `notebook.js:38`), landing the file in the jailed `WORKSPACES/<agentId>/`, viewable via the read-only `GET /api/file?agent=<id>&path=<rel>` route (`index.js:355-378`). Hermes's `deliver:'local'` = "persist a file, emit nothing" (`scheduler.py:432-433,676`); gen's local sink reuses the existing tools + `/api/file` verbatim — the ROUTINES panel renders the link from the last-result record.
- **(c) In-app NOTIFICATION — reuse the persisted `notify` rung.** `StationUI.notify` pushes `{id,t,txt,cls,read:false}` to `store.notifs` (`stationui.js:440-445`) and bumps the unread badge via `badges()` (`:459-463`). **Honest catch:** `store.notifs` is browser-side; the sidecar can't write it while the app is closed. The honest pattern: the sidecar records the result server-side (the `cron.result` rung + the job's `lastStatus`/`lastReason`/`lastError`), and on next app open the ROUTINES panel reconciles **unseen results — successful AND failed — into `notify()`** (the badge appears next open because the *server* has the result, not because we faked a notification). **The reconcile is symmetric:** an errored run or a failed delivery surfaces as a **red/`warn` notify cue + the `lastError` text in the row**, not only successes as a green cue — the §3.9 honesty mandate ("never a green last-run ok without a real result") must equally guarantee a *visible failure when one occurred*, so an unattended job that errored or whose alert failed to deliver is flagged when the user returns rather than buried in a log line.

**Delivery-FAILURE behaviour (not just the failure signal) — output is never lost.** §3.8 defines `cron.result{outcome:'failed'}` as the failure *signal*; this is the failure *behaviour*:
- **(a) Persist BEFORE send.** The run OUTPUT is always recorded server-side — the job's last-run record (the reply text + `lastRunId`/`lastReason`) and the local deliverable — **before** any channel send is attempted, so a failed send never loses the work (mirror Hermes saving output at `scheduler.py:2052` before the delivery decision). Delivery is a step *after* the result is safe, never a precondition for keeping it.
- **(b) On `adapter.send {ok:false}`** (after the one bounded resend the messaging plan allows, `MESSAGING:98`): emit `cron.result{outcome:'failed', reason}` **AND** record `lastStatus:'error'`/`lastError` on the job, so the ROUTINES panel surfaces the failed delivery on next open (§3.6c symmetric reconcile) — not just a transient log line.
- **(c) Local-sink failure path.** If the deliverable `fs.write` is **consent-denied** under `surface:'autonomous'` (the §3.5 default-deny), the run **still records the reply text in the last-run record**, so the result is recoverable via `GET /api/file` or the panel row — never silently dropped. A denied write degrades the *sink*, not the *result*.

**The [SILENT] no-spam check — a pure post-run check on the final assistant text.** Hermes prefixes every cron prompt with a hint ("if there is genuinely nothing new to report, respond with exactly `[SILENT]`", `scheduler.py:1133-1144`) and does a post-run check in `_process_job` (`:2064-2066`): if the final text contains `SILENT_MARKER`, **skip delivery but keep the saved audit doc** (the output is saved at `:2052` before the check). gen's equivalent: the producer half is one sentence in the job's `system` prompt; the consumer half is **one line at the `agent.run.end` reassembly point**, on the buffered text. It is pure (operates on a string already in hand — no clock, no I/O — passes `lint-determinism`). On SILENT: skip `adapter.send`/skip the `channel.delivery` emit (or emit `ok:true,chunks:0,reason:'silent'`), still persist the run output. **Deliberate divergence (flag):** use **strict-equals after trim** (`reply.trim() === '[SILENT]'`), **NOT** Hermes's `contains` substring check (`scheduler.py:2064`) — a contains-check lets `[SILENT]` embedded in a real report accidentally suppress it. **Failed jobs always deliver** (Hermes `:2059` — a broken alerting job is never silent); SILENT applies only on success. Skip Hermes's *second* layer (the `delivery.py` silence-narration regex for multi-bot mirror loops) — gen has no such loops; record it as a future extension point.

### 3.7 Secrets / config frozen at boot

The two new secrets a cron run needs, **frozen at boot in `index.js`** (the only ambient-I/O module), mirroring `FULL_ACCESS`/`SKYNET_FULL_ACCESS` (`index.js:73`) and shared verbatim with `MESSAGING §3.5`:

| Secret | Source | Residence |
|---|---|---|
| OpenRouter **key** | `process.env.SKYNET_OPENROUTER_KEY` (frozen `const` at boot) | Threaded into `runOnce`'s provider+web-tools exactly as the browser's `key` (`index.js:187,216`). **Never** placed on a bus event; never embedded in a persisted `CronJob` (`redact()` exists precisely to keep keys off disk/wire, `index.js:156`). |
| Default **model** | `process.env.SKYNET_DEFAULT_MODEL` (frozen at boot) | Supplied as `model` to `runOnce` when the job's `model` is null. |

If neither is configured at boot, the cron subsystem **stays off** (no `setInterval`), and the existing browser path is entirely unaffected — cron is opt-in via env. A persisted `CronJob` must **never** embed the API key; the schedule fires, the key comes from the boot-frozen source.

> **No-jobs invariant (distinct from no-secrets).** Even when the env gate is **on**, if `cron.jobs.json` is empty or absent the feature is effectively off: the tick is a **pure no-op over an empty array** — it mints **no `runId`**, calls `runOnce` **zero times**, and emits at most a `cron.tick{due:0}` (or, to honor the invariant strictly, emits nothing and arms the timer lazily on first job create). An empty store changes **no** browser-path behaviour and incurs **no** run cost. So the feature stays entirely off / browser path untouched in *both* conditions: no secrets **and** no jobs.

> **Cumulative/daily SPEND-CAP gap (flag, do not necessarily build now).** Every cost ceiling today is per-run: `CAPS.maxCostUsd:1.00` (`index.js:31,275`), guarded at `loop.js:131`; `spentUsd` is loop-local and resets each run. There is **no** process-wide/per-day accumulator anywhere in `sidecar/` (confirmed by absence). For directive runs this is acceptable — a human watches. **Cron removes the human**, so the gap graduates to a genuine unattended-runaway hole: an hourly job at the default cap is up to **$24/day**; a misconfigured `* * * * *` job is up to **$1,440/day** — every individual run obeyed its cap. Hermes cron has the *same* gap (no USD cap at all, `scheduler.py`; it even markets "Unlimited" runs/day). The eventual fix is its own ≥1-commit step **after** the basic schedule store + autonomous broker: a daily spend ledger `WORKSPACES/cron.spend.json` (`{version:1,day:'YYYY-MM-DD',usd}`, atomic persist) and a pure injected-clock "is the day rolled over / is the cap blown?" function with a gating test (the wall-clock read stays in `index.js`). **Reuse the already-frozen rung, don't invent one:** `shared/events.js:64` already defines `budget.threshold{scope ∈ ['run','day','global'], usd, cap}` — the daily spend-cap step emits `budget.threshold{scope:'day',usd,cap}` for the threshold/exhaustion signal rather than adding a new `agent.budget.exhausted` rung (that would be a redundant frozen event; "every cross-boundary signal gets a rung, prefer the existing one"). Do **not** fold it into the first cron commit.

### 3.8 New `shared/events.js` cron.* rungs (added BEFORE producers)

The cron lifecycle is genuinely new cross-boundary signal; `emitter.js` drops any unregistered name (`:17,20-23`) and `lint-emits.js` flags the literal `emit` (`:37,45`). The *run trigger itself needs none* — `agent.run.start.trigger:'schedule'` is already frozen (`events.js:24-26`). We add **five** rungs *first* (additive, no re-freeze of the run/reply path), payloads kept to the primitives `schema.js` supports (`type`/`enum`/`properties`+`required`/`items`):

```js
// ---- cron / scheduler (cross-boundary; deterministic times come from the injected clock) ----
'cron.tick':    obj(['at','due'], { at: int, due: int }),
'cron.fire':    obj(['jobId','runId','at'], { jobId: str, runId: str, at: int, scheduledFor: int }),
'cron.skipped': obj(['jobId','at','reason'],
                  { jobId: str, at: int, reason: { enum: ['already-running','disabled','caught-up','no-capability','stale-lock-reclaimed'] } }),
'cron.next':    obj(['jobId','nextAt'], { jobId: str, nextAt: int, prevAt: int }),
'cron.result':  obj(['jobId','runId','outcome'],
                  { jobId: str, runId: str, outcome: { enum: ['delivered','failed','no-op'] }, reason: str }),
```

- `cron.fire` carries `runId` so the existing run stream (`agent.run.start`/`.end`, `agent.cost`) correlates to the job **without** a new field on those frozen events. The fire is the *bridge* event; the run itself stays on the unchanged `trigger:'schedule'` contract.
- `cron.result` is distinct from `agent.run.end`: `run.end` reports the loop outcome; `cron.result` reports the *job-level* outcome including the delivery step (`no-op` = `[SILENT]`/local-only; `delivered`/`failed` = channel result). Its `reason` reuses the frozen `agent.run.end` vocabulary so the HUD's last-result labels match the `endReason` labels at `chat.js:187-190` (`'reached the step limit — say "continue"…'` / budget / cancelled). (The reply itself is reassembled from `agent.token` deltas at `harness.js:91` `full += payload.delta`, cited correctly in §3.4 — `chat.js:163-168` are the stream callbacks, not the reassembly point.)
- `at`/`nextAt`/`scheduledFor` are `int` ms epoch from the injected clock — never `Date.now()` inside backend logic.

Each rung lands with **valid + invalid `contract.test.js` fixtures** in the same commit, *before* its first producer — the discipline `HERMES_INTEGRATION_PLAN.md:284` and the `MESSAGING_INTEGRATION_PLAN.md:176-188` precedent both mandate. Adding rungs is a source edit to the frozen `EVENTS` object in a dedicated commit, exactly as the L2 `tool.args.repaired` rung was added after the original freeze.

### 3.9 The frontend ROUTINES panel + endpoints + whether a routine owns a workstream

**The panel** slots into the existing five-seam pattern: a `data-term="routines"` button beside the five at `index.html:117-121` (with an unread badge mirroring `nf-badge`, the span at `:121`), and a `buildRoutines(body)` registered in `BUILDERS` (`stationui.js:479-485`) — modeled on `buildTasks` (`:267-292`) for the list/CRUD shell and `buildSettings` (`:406-437`) for the arm/confirm-delete idiom (the two-step arm/confirm guard `:432-435` copied verbatim; the **no native dialogs** comment is at `:430`). A row shows: name, schedule (human + raw), enabled toggle, **next-fire**, **last-result** (ok/error + timestamp), and `▶ RUN NOW · ✎ EDIT · ⏸ ENABLE/DISABLE · ✕ DELETE`.

**Crucial divergence from the TASKS panel — persistence ownership.** TASKS stores cards in `localStorage` (`stationui.js:240`) because a card is a client-only convenience. **Routines must NOT** — the job *definitions live server-side* (schedule+secrets are host-owned, never on the bus, §3.7). The panel is a **thin CRUD client over new sidecar endpoints** (exactly as `buildSettings`'s key editor is a CRUD client over the Harness BYOK store, `wireKeyActions:373-403`): render from a GET snapshot → mutate via POST → re-fetch and `rerender('routines')`. Cosmetic UI state (which rows are expanded) may live in `starnet.station.v1`; the source of truth is the server. **Honesty mandate:** like the SKILLS panel labels not-yet-built capability as "○ LOCKED" (the `'○ LOCKED'` literal rendered at `:229`, from the `on:false` skill rows at `:217-218`), the panel never shows a "next fire" the sidecar didn't compute or a green "last run ok" without a real result record.

**Endpoints** (mirror the `/api/*` shape; read-only routes beside `index.js:114-115`, 127.0.0.1-bound). The panel learns next-fire/last-result by **pull, not listen** — the NDJSON `/api/run` stream is request-scoped and a fire happens with no stream (symmetric with `Harness.listModels` doing a plain `GET /models`, `harness.js:35-49`):

1. `GET /api/cron` — list (include disabled; let the UI filter) → `{ jobs:[{id,name,schedule,scheduleDisplay,enabled,nextRunAt,lastRun:{t,reason,ok,error,runId,cost,deliverables:[{title,kind}]},workstreamId}] }`. `reason` reuses the frozen `events.js:46` enum.
2. `POST /api/cron` — create → `{ok, job}`.
3. `PATCH /api/cron/:id` — update + pause/resume via an `enabled`/`state` field (folds edit+pause+resume).
4. `DELETE /api/cron/:id` — remove → `{ok}`.
5. `POST /api/cron/:id/run` — **run-now**, streaming NDJSON exactly like `/api/run` with `trigger:'schedule'`. **Strictly better than Hermes**, whose `cron run` only sets `next_run_at=now` and waits for a tick (`jobs.py:827-841`); gen's run-now is a real, watchable, synchronous run.
6. `POST /api/cron/preview` — validate + **next-N-fire-times** → `{ok, kind, display, next:[…5 ISO times]}`. **Net-new GUI value Hermes lacks entirely** (no preview/dry-run/"next 5" anywhere in its cron path); `nextRunAt` must be computed with the injected clock, never bare `Date.now()`.

**Should a routine own a workstream? — YES: a Routine OWNS a workstream (one stream per routine), filing each fire's run/cost/deliverables via `appendRun`/`addCost`/`recordDeliverable`.** A Workstream is "one named conversation that OWNS its backend runs + deliverables + per-conversation cost" (`workstreams.js:1-5`) — exactly a long-lived routine accruing many fires. The plumbing exists and is unit-tested: `appendRun(id,runId)` auto-advances `todo→active` on the first real fire (`:160-166`, the hybrid-honest lane — a routine that *has* fired *is* active); `addCost` (`:173-179`); `recordDeliverable` (`:167-171`). **Reject "a Routine BE a workstream"** — the schedule + secrets are server-owned and must never serialize into the browser localStorage `Workstreams` slice (`serialize():90`), which would leak a secret into a redaction-free store.

> **Load-bearing caveat (flag) — corrected.** The browser-side Workstreams wiring **does** exist and is live: `chat.js:163` calls `Workstreams.appendRun(ws.id, id)` on `onRunId`, `:172` `Workstreams.recordDeliverable(ws.id, …)` on `onDeliverable`, and `:205` `Workstreams.addCost(ws.id, …)` in the run's `finally` — real calls inside the run/deliverable/cost handlers, not migration refs (`index.html:138` loads the module; `save.js:16-19` wires migration). So the §3.9 conclusion ("a Routine OWNS a workstream") is **better** supported than a "dormant / slice-2 unwired" reading would suggest. The honest remaining gap is **narrower and different:** that wiring is **browser-side** — `chat.js` drives `appendRun`/`addCost`/`recordDeliverable` off the **NDJSON stream of an interactive run**, and the `Workstreams` store is **client-side / `localStorage`-only** (`serialize():90`). A cron fire happens with **no browser and no stream**, so the server *cannot* call these browser-only methods. The real reason a routine needs a **server-side per-job stream record** (id + runIds + cost + deliverables, reconciled into the browser `Workstreams` model on next app open) is therefore "the Workstreams store is client-side and a headless fire never reaches it," **not** "slice-2 hasn't landed." The working-now option stands on that correct basis.

### 3.10 Blueprints + honest suggestions UX

**The Routines catalog (= Hermes blueprints).** Hermes's load-bearing choice: **users never type raw cron** (`blueprint_catalog.py:18-21`); a `schedule_template` carries `{slot}` placeholders and only human-friendly parts (time-of-day, weekday-set, interval) are parameterized. A blueprint is a typed-slot front-end over the **one real `create_job`** ("so there is no second job engine", `:14`), instantiated by the pure `fill_blueprint` (`:661-713`) which validates values, **rejects unknown slot names** (`:675-681`), and returns a create-job kwargs dict. gen's direct re-derivation, house-style (the precedent is `frontend/app/workstreams.js` — a pure, UMD, node-testable module): a pure `routines.js` exporting a typed-slot `Routine` schema (`time`/`enum`/`text`/`weekdays` slots, like `BlueprintSlot:50`) + a small static catalog + a pure **`fillRoutine(routine, values) -> runSpec{trigger:'schedule'}`** that returns the **same options object `runOnce` takes** — **one run engine, no second schema**. The static catalog passes `lint-determinism` trivially (no clock/rng).

**What to KEEP, generalized (drop the lifestyle accretion).** Hermes over-accreted 14 personal-assistant blueprints (`blueprint_catalog.py` — 14 `AutomationBlueprint(` entries); gen's acceptance test, paraphrasing the domain-agnostic acceptance rule at `rpg-layer-design.md:55` ("a real thing a general agent harness does" that can "be made domain-agnostic, [or] it doesn't ship"), is a 1:1 wrapper over a real agent-ops action that works for ANY use case. **DROP** the domain-locked wellbeing/lifestyle entries (`meal-plan`, `habit-checkin`, `hydration-move`, `gratitude-journal`, `learn-daily`, `bill-renewal-watch`, `on-this-day`, `evening-winddown`). **KEEP ~5 domain-agnostic**, generalized: a scheduled **briefing** (over connected sources/Workstreams, not "weather+calendar"), a scheduled **recap of what the station shipped** (a pure reducer over real deliverables — honest by construction), a **`[SILENT]`-gated monitor** (poll → score → surface only above threshold — the genuinely reusable engine, mirror `important-mail`), a deduped **topic digest** (web_search already exists), and a **custom prompt on a schedule** (the universal escape hatch). **DROP the multi-renderer sprawl** — Hermes emits four renderings (form/slash/deep-link/docs, `:492-589`); gen has one surface that matters now (the in-app form), so keep `routineFormSchema` + the filler, skip the slash-stringifier / `hermes://` deep-link / docs generator until a CLI/messenger/docs surface consumes them. **DROP** the `strict=False` open-enum cleverness on the deliver slot — a closed enum of real targets is honest.

**Honest suggestions (the anti-nag store).** Hermes surfaces "ready-to-run cron job specs" the user accepts (creates the real job) or dismisses (latched, never re-offered), from four sources (`suggestions.py:56`). **Ship only the honest subset:**
- **`catalog` (KEEP):** seed the Routines catalog as the new-user suggestion source — honest because it's framed "here's what you could set up," not "we noticed you…" (mirror `seed_catalog_suggestions:124-154`).
- **`usage` (FAST FOLLOW, honest-only):** a suggestion allowed **only** when it's a pure reducer over the real bus event log / Workstream run history ("you've run a directive matching X 3 times this week — promote to a schedule?", with the drill-to-the-run tooltip the RPG doc makes the moat). **No invented data**: no recurring real run, no suggestion. Gate on Workstreams shipping so there *is* a run history to reduce.
- **DROP `integration`** (no connected-account event) and **`blueprint`-from-skill** (gated on the deferred skill layer, HERMES L8 → RPG-2/3).

**The anti-nag machinery is the part to copy faithfully** (`suggestions.py:54,150-160`): `MAX_PENDING=5` cap ("so the list never becomes a nag wall" — **drop when full, don't queue**); the dedup-latch (a dismissed **or** accepted `dedup_key` is **never re-offered**); consent-first acceptance (accept → `fillRoutine` → a scheduled run, which **still** passes the autonomous-surface default-deny gate per run — accepting a suggestion can never silently grant a mutation). This aligns with gen's bright lines: `rpg-layer-design.md:109` ("a single dismissible suggestion"), `:122` ("any re-engagement ping is forbidden"). The suggestion store is save-safe (atomic temp+rename, sibling of the fs jail), and any suggestion-surfaced/accepted/dismissed signal that crosses the bus to the HUD gets a `shared/events.js` rung before its producer.

> **Flag:** Hermes's `usage` heuristic is a *declared contract*, not shipped code in the files read (`suggestions.py:14-15` declares it; only the static `seed_catalog_suggestions` ships). Treat gen's `usage` reducer as a **new honest build** (a pure fold over real Workstream history), not a port.

---

## 4. Drop section — Hermes/Python accretion left in the mine

Mined for shape, **not** ported (with the disproven/irrelevant premise where it applies):

- **The `ThreadPoolExecutor` parallel pool + single-thread sequential workdir pool + `as_completed` + `_submit_with_guard`** (`scheduler.py:164-204,2101-2154`). Node has no GIL and runs one agent at a time through `runOnce`; "parallel vs sequential pool" collapses to in-process serial dispatch. The per-job in-flight skip is the only piece kept (a `Set<jobId>`).
- **`croniter`** (`jobs.py:27-31`). gen is zero-dep; a full cron engine is not free. Support `interval`+`once` first; 5-field cron is a deferred hand-rolled subset (`*`, ranges, lists, steps), explicitly not a croniter port. The cron-based grace period-measurement (`jobs.py:358-366`) is deferred with it; interval grace (`period/2`) is trivial and dependency-free.
- **IANA `ZoneInfo` resolution + the `HERMES_TIMEZONE`/config tz cascade + the `_cached_tz` cache + `_ensure_aware` naive-coercion** (`hermes_time.py:37-115`, `jobs.py:295-311`). gen is a local desktop harness; server-local time (a single injected `clock.now()`) is the honest v1 default. Keep only the seam (next-fire vs an injected `now`).
- **The `child_process` script-preprocessor (`_run_job_script`) + its path-jail + interpreter-by-extension + 120s timeout + `redact_sensitive_text` + the downgraded assembled-prompt injection scanner + the `wakeAgent` gate + `no_agent` mode** (`scheduler.py:892-1030,1236-1303`). **Disproven premise for gen:** a pre-run script is `subprocess.run` — a brand-new ambient-I/O *class* (arbitrary code execution) that would bloat the one edge module and bypass the capability projection entirely. gen's honest substitute: the scheduled agent collects data **through the existing capability-gated tools** (`web_fetch` SSRF-guarded, `fs.read` jailed, `notebook.read`) with `isTask:true` — the data-into-context goal met *inside* the consent model, and the "stay silent if nothing" outcome is the §3.6 `[SILENT]` check. A true no-LLM scripted watchdog is a separate later decision with its own ambient-I/O placement, path-jail, kill, redaction, and consent gate — gated on an explicit user requirement, not the MVP.
- **The webhook/HMAC trigger path** (Hermes's GitHub-webhook/API triggers converge on `run_job` but ride `TELEGRAM_WEBHOOK_SECRET`-style crypto + a public listener). gen binds `127.0.0.1` (`index.js:124`); the `'event'` trigger enum value exists but a webhook ingress is out of scope here — long-poll/local-timer only.
- **Multi-platform delivery beyond the channels plan:** the `_HOME_TARGET_ENV_VARS` map, the `_KNOWN_DELIVERY_PLATFORMS` whitelist (18 platforms, `scheduler.py:116-142`), the `Platform` enum, the live-adapter/standalone-loop dual-send + `asyncio.run` of send coros, and the `MEDIA:`-tag / `wrap_response` header machinery (`scheduler.py:659-851`). gen reuses the messaging plan's single `adapter.send` seam. (**Flag:** `github_comment` is **not** a Hermes cron deliver target — GitHub lives in the webhook/gateway path; the task's target list over-included it. `--deliver` **is** a real registered CLI flag (`hermes_cli/subcommands/cron.py:38` on `cron create`, `:82` on `cron edit`) **as well as** the `deliver` job/tool field — both forms exist; gen folds the value into the `CronJob.deliver` field either way.)
- **The `HERMES_CRON_SESSION` env flag + `HERMES_CRON_AUTO_DELIVER_{PLATFORM,CHAT_ID,THREAD_ID}` contextvars + `set_session_vars` clearing + `platform="cron"` + SOUL.md/SessionDB wiring** (`scheduler.py:1487-1578,1747-1756`). gen needs none: `trigger:'schedule'` is a first-class param on `agent.run.start` (already frozen) — an observable, replayable fact, not ambient process state.
- **The inactivity-timeout poller (activity tracker, `get_activity_summary`, `agent.interrupt`), the prompt-injection scanner, MCP-orphan reaping, credential-pool/provider-fallback, the three-layer nested try/except** (`scheduler.py:1759-1914`, default `HERMES_CRON_TIMEOUT` 600s). gen's honest core for the hang/runaway case is **`toolTimeoutMs:30000` × `maxIters:16` ≈ 8-min worst-case bound** (`index.js:31`, `sidecar/tools/registry.js:33`) — NOT `maxIters`/`maxCostUsd` alone, which do not advance while a tool hangs — plus the §3.3 self-healing lease that aborts a zombie that exceeds `CRON_MAX_RUN_MS`, the existing `agent.run.error{transient}` rung + the L3 classifier, and a single try/catch per fired run isolating a throwing job. A Hermes-style run-level idle kill is a deliberate omission raised in §6 Decision #10.
- **The suggestion-engine accretion:** the `integration`/`blueprint`-from-skill sources, the `usage` mining heuristic that ships nowhere (a declared contract only, `suggestions.py:14-15`), and the four-renderer blueprint sprawl. Keep the `catalog` source + the anti-nag machinery (`MAX_PENDING`, dedup-latch, consent-first) faithfully; build `usage` as a new honest fold when Workstreams ships.
- **Threading/asyncio machinery generally:** `threading.Lock _jobs_file_lock` (`jobs.py:44`), the cross-process `.tick.lock` fcntl/msvcrt (`scheduler.py:231-235`), `asyncio.run` delivery (`scheduler.py:825,833`). Node single-threaded async: a `setInterval` + a `Set` + a `Map` is the whole mechanism; no second ticker process exists to lock against.
- **Hermes's `contains` `[SILENT]` check** (`scheduler.py:2064`) — gen diverges to strict-equals-after-trim (a contains-check lets an embedded `[SILENT]` suppress a real report). The second-layer `delivery.py` silence-narration regex (multi-bot mirror-loop guard) is dropped; gen has no such loops.
- **The strict=False JSON-repair ladder / `RuntimeError("corrupted and unrepairable")` + the no-version `jobs.json` envelope** (`jobs.py:423-468,477`). gen's store is simpler/stricter: load → corrupt → `[]` fail-closed (the `index.js:81` allowlist idiom) + a `{version:1}` envelope (the gen addition Hermes lacks).

---

## 5. The commit sequence (dependency-ordered, smallest-first, each independently shippable + test-gated)

Each is one revertable commit, ≤3 files, gating test written first, fast suite green (`<~10s`) before the next. The pattern matches `HERMES_INTEGRATION_PLAN.md §4` and `MESSAGING_INTEGRATION_PLAN.md §5`: start with a **pure, no-wiring** cron-math module + its test, then the store, then the schema rungs, then **the `runOnce` extraction (4a) and the tick wired to it behind an env gate (4b)**, then delivery, then the UI panel. (The original "six commits" is now seven steps because Commit 4 splits into 4a/4b — see the blocker note below; 4a collapses to a no-op if MESSAGING ships the extraction first.)

**Commit 1 — `cron.js` pure cron-math + its test (no wiring).**
- *Goal:* the deterministic schedule core — `parseSchedule(str,now)` (interval+once+ISO+duration; cron-expr deferred), `nextFireAt(schedule,lastRunIso,now)`, `dueJobs(jobs,now)` (the fire set **and** the advanced next-fire — the no-backlog pair), `planTick(jobs,now)` returning `{fire,skipped,next}` as data — all injected-clock, zero-dep, lint-clean.
- *Port-create:* `sidecar/cron.js`, `test/cron.test.js`, `package.json`.
- *Tests:* `cron.test.js` with `makeClock` (`clock-rng.js:23`) — `parseSchedule` returns the right tagged union for "every 30m"/"in 2h"/ISO/`2h`; nothing due at t=0, an interval job due after one period; **at-most-once across a simulated crash** (planTick → persist advance → "restart" same clock → no double-fire); a stale-by-more-than-grace recurring job fast-forwards + skips, not a backlog burst; `JSON.stringify(planA)===JSON.stringify(planB)` byte-identical (`loop.replay.test.js:76` shape).
- *DoD:* pure (injected clock, no `Date.now`/`setTimeout`/rng/`fs`), `lint-determinism` green, headless, deterministic; nothing wired; one commit, ≤3 files; revertable.

**Commit 2 — `cron-store` reducer + the save-safe job store (no tick wiring).**
- *Goal:* the pure lifecycle reducer (`create`/`update`/`pause`/`resume`/`markRun`/`remove` over the job array, timestamps+ids injected) + the load/persist of `WORKSPACES/cron.jobs.json` (the `index.js:76-88` allowlist idiom: `{version:1,jobs}`, atomic temp+rename, load→corrupt→empty fail-closed, persist throws).
- *Port-create:* `sidecar/cron-store.js` (pure reducer), `sidecar/index.js` (load/persist beside `persistAllowlist`), `test/cron-store.test.js`.
- *Tests:* create rejects a bad schedule; `update` rejects the immutable `id` and recomputes `nextRunAt` on schedule change unless paused; `markRun` writes status/reason and deletes on repeat-exhaustion; a recurring job that can't compute next-run goes `state:'error'` but stays `enabled`; store round-trips, returns `{version:1,jobs:[]}` on missing/corrupt, lives **outside** the fs jail; persist throwing surfaces to the caller.
- *DoD:* reducer pure/clock-free/rng-free; the store load/persist is the only fs, in `index.js`; one commit, ≤3 files; revertable.

**Commit 3 — `cron.*` event rungs (schema first, no producer).**
- *Goal:* freeze `cron.tick`/`cron.fire`/`cron.skipped`/`cron.next`/`cron.result` in `shared/events.js` **before** any emit, so producers in later commits are lint-clean.
- *Port-create:* `shared/events.js` (five additive entries, §3.8), `test/contract.test.js` (valid + invalid fixture per rung).
- *Tests:* each new event validates a good payload and rejects a bad one (missing required / bad enum); `lint-emits.js` stays green; existing frozen events untouched (no re-freeze of the run/reply path).
- *DoD:* additive only; fast suite green; one commit, ≤2 files; revertable.

**Commit 4 — wire the autonomous tick. SPLIT into 4a (the `runOnce` extraction) + 4b (the tick driver), because `runOnce` does not exist yet.**

> **HARD PREREQUISITE (blocker, verified).** Grep for `runOnce` across `sidecar/` returns **zero** matches — it is **not built**. `index.js:184-281` is still the monolithic inline run core; `MESSAGING_INTEGRATION_PLAN.md` is at Commit 1 only (only `sidecar/channels/adapter.js` exists; no hub/store/transport; no `channel.*` rungs in `events.js`), so **MESSAGING Commit 4 (the extraction) has NOT landed.** CRON Commit 4 therefore **cannot "ride `runOnce`"** — it must perform the extraction itself. Folding the extraction + the tick driver + boot-frozen secrets + the in-flight lease into one commit blows the ≤3-file / one-concern budget. Hence the split; **4a is a hard prerequisite** for 4b whenever MESSAGING hasn't shipped it (and if MESSAGING *does* ship it first, 4a collapses to a no-op and 4b proceeds directly).

**Commit 4a — extract `runOnce` from `handleRun` (behavior-preserving; no cron yet).**
- *Goal:* refactor the inline run core (`index.js:184-281`) into `runOnce({key,model,system,messages,agentId,isTask,emit,signal,runId,surface,trigger,prompt})`, leaving `/api/run` (`handleRun`) as a thin caller that mints `runId`+`ac` and passes the browser's `surface:'interactive'`+`trigger:'directive'`+`promptConsent`. Add the four params cron needs (**B1/B2**): `surface`, `trigger`, `runId` (closed over from `:148`), optional `prompt` (closed over from `promptConsent`, `:163-181`). **No behavior change** for the browser path.
- *Port-create:* `sidecar/index.js`, `test/harness.integration.test.js` (the regression lock).
- *Tests:* the existing `harness.integration.test.js` stays green byte-for-byte (the extraction is behavior-preserving); the new params default to today's hardcoded values when omitted (`surface:'interactive'`@`:211`, `trigger:'directive'`@`:222,:277`), and `harness.integration.test.js:132-140` (the autonomous default-deny) still passes via the extracted path.
- *DoD:* `/api/run` is a thin caller; `runOnce` is the single run host; existing suite green; one commit, ≤2 files; revertable. (If MESSAGING Commit 4 already shipped this, 4a is skipped.)

**Commit 4b — the scheduler tick + boot-resume + self-healing lease, wired to `runOnce` behind an env gate.**
- *Goal:* the seam — boot-frozen secrets (§3.7); on boot, when `SKYNET_OPENROUTER_KEY`+`SKYNET_DEFAULT_MODEL` are set, **load `cron.jobs.json` and run ONE immediate `applyTick(Date.now())` (the resume reconcile) BEFORE registering a `setInterval(60s)`** that thereafter calls `applyTick(Date.now())`. Each tick: at the top, reclaim any stale lease (`Map<jobId,startedAtMs>` older than `CRON_MAX_RUN_MS` → `abort()` + `cron.skipped{stale-lock-reclaimed}`); run pure `planTick`; persist advances; for each fire check the lease, mint `runId`, build the runSpec, call **`runOnce({...,surface:'autonomous',trigger:'schedule'})`** with an in-process reply-assembling `emit`; release the lease in a `finally` on every terminal path; emit `cron.tick`/`cron.fire`/`cron.skipped`/`cron.next`. Browser path untouched when the gate is off.
- *Port-create:* `sidecar/index.js` (timer + boot-frozen secrets + resume tick + tick driver + lease `Map`), `test/cron.tick.test.js`.
- *Tests:* a fake clock + a fake `runOnce` (emitting `agent.run.start`/`agent.token`×3/`agent.run.end{done}`) → the due job fires exactly once, `nextRunAt` advanced **before** launch, `cron.fire` carries the run's `runId`, **and the fire passes `surface:'autonomous'`** (asserted, per §3.4); a job already leased is skipped (`cron.skipped{already-running}`); **a fired run that never emits `run.end` → the next tick after the lease window reclaims the lock + aborts + the job becomes eligible again** (`cron.skipped{stale-lock-reclaimed}`); **a recurring job persisted with `nextRunAt` in the past + a restart advanced past one grace window → exactly one catch-up fire (no backlog) + the timer armed** (the resume test); a throwing fired run records `lastStatus:'error'` and the next job still fires; an ungranted-mutation run under `surface:'autonomous'` completes (write silently denied, run recovers).
- *DoD:* timer/now-source/id/secrets/lease all in `index.js` (the lint-exempt edge); cron-math/reducer stay pure; messaging-style env gate; **the resume reconcile runs before the interval is armed**; **the fire asserts `surface:'autonomous'`**; the lease releases on every terminal path; browser path byte-identical when off; one commit, ≤3 files; revertable.

**Commit 5 — DELIVERY (the [SILENT] check + local/in-app sinks; channel sink behind the messaging gate).**
- *Goal:* **persist the run output server-side FIRST** (last-run record + local deliverable, §3.6), then run the pure strict-equals `[SILENT]` check on the buffered reply (suppress delivery, keep audit); for `deliver:'local'`/in-app, emit `cron.result{outcome}` + the existing `notify` rung (reconciled into the HUD on next open); for `deliver:'channel:<id>'` (only when the messaging hub exists) chunk via `adapter.send` + emit `channel.delivery`. On `adapter.send {ok:false}` (after the one bounded resend) emit `cron.result{outcome:'failed',reason}` **and** record `lastStatus:'error'`/`lastError`. Failed jobs always deliver; SILENT only on success.
- *Port-create:* `sidecar/index.js` (the reply-assembler's end-of-turn delivery branch + `[SILENT]` check), `sidecar/cron-deliver.js` (pure: decide sink + chunk + the strict `[SILENT]` predicate), `test/cron.deliver.test.js`.
- *Tests:* `reply.trim()==='[SILENT]'` → no `adapter.send`/no `channel.delivery`, output still persisted, `cron.result{no-op}`; a normal reply → `notify` + `cron.result{delivered}` (or chunked `adapter.send` when a channel is configured); a failed run delivers the error (never silent); an embedded-`[SILENT]`-in-a-real-report does **not** suppress (strict-equals divergence asserted); **`adapter.send {ok:false}` → the run output is still persisted (recoverable via `/api/file`/the panel) + `cron.result{outcome:'failed'}` emitted + `lastError` set on the job** (the §3.6 delivery-failure behaviour); a consent-denied local `fs.write` under `surface:'autonomous'` → the reply text still lands in the last-run record (never silently dropped).
- *DoD:* output persisted before any send; the `[SILENT]` predicate + sink decision pure; reuses `notify`/`deliverable`/`adapter.send` — no second sender; one commit, ≤3 files; revertable.

**Commit 6 — the ROUTINES panel + `/api/cron` endpoints + preview/run-now.**
- *Goal:* `GET/POST/PATCH/DELETE /api/cron`, `POST /api/cron/:id/run` (streamed, `trigger:'schedule'`), `POST /api/cron/preview` (validate + next-5 fire times); a `buildRoutines` panel (CRUD client over the endpoints, server is source of truth) with the next-fire/last-result row, the two-step delete guard, and the on-open notify reconciliation.
- *Port-create:* `sidecar/index.js` (the `/api/cron*` routes beside `:114-115`), `frontend/app/stationui.js` (`buildRoutines` + the `data-term="routines"` button registration), `test/cron.api.test.js`.
- *Tests:* `GET /api/cron` returns the job snapshot with `lastRun.reason` from the frozen enum; create/patch/delete round-trip through the store; `POST /api/cron/preview` returns 5 ISO next-fire times for "every 1h" (computed via the injected clock); run-now starts a real streamed run with `trigger:'schedule'`; the panel renders next-fire/last-result only from server data (no faked values).
- *DoD:* endpoints 127.0.0.1-bound, read routes additive; preview/next-fire uses the injected clock; panel is a thin CRUD client (no localStorage source of truth for definitions); one commit, ≤3 files; revertable.

> Commits 1–3 are pure substrate (cron-math, store, schema) — independently shippable, zero behavior change. Commit 4 is split: **4a** extracts `runOnce` (behavior-preserving, regression-locked by `harness.integration.test.js`; skipped if MESSAGING shipped it first) and **4b** is the live autonomous tick (boot-resume + self-healing lease) behind an env gate. Commit 5 is delivery + the no-spam check + the delivery-failure behaviour. Commit 6 is the GUI. Nothing touches `loop.js`'s while-loop, `cost.js`, the capability projection, the reply contract (`agent.token`), the autonomous-consent posture, or the frozen events beyond the additive `cron.*` rungs. **Deliberate follow-ons, sequenced after Commit 4b, not folded in:** the cumulative spend ledger (§3.7, reusing `budget.threshold{scope:'day'}`), transient-retry/backoff (§3.3, §6 Decision #11), and a run-level wall-clock/idle kill if adopted (§6 Decision #10).

---

## 6. Open design decisions that need the user's call

1. **Schedule input format (carries the tz/DST obligation).** Ship `interval` + `once` (`every 30m` / `in 2h` / ISO timestamp) for v1 and defer 5-field cron to a later hand-rolled subset (`*`, ranges, lists, steps — not a croniter port)? Or invest in the cron subset up front? Interval+once covers the bulk of real routines, is zero-dep today, and is **duration-anchored so DST never bites it** (§3.1). The deferral consciously carries the tz/DST obligation: the deferred time-of-day/cron-expr subset MUST resolve against a tz-aware `now` and ship its own spring-forward/fall-back test (§3.1). Confirm interval+once for v1.

2. **Missed-run / catch-up policy.** Confirm fast-forward-and-fire-one-catch-up-within-grace (the Hermes no-backlog default, §3.3) vs run-each-missed (a backlog burst on restart) vs skip-all-missed (never catch up). Fast-forward-one is proposed and is the safe default for an unattended host.

3. **Overlap policy.** A job still running when its next tick arrives: **skip** (at-most-once-in-flight, the Hermes default, §3.3) vs **queue** (run after the current finishes) vs **abort-and-restart** (the messaging abort-on-new semantics). Skip is proposed (a routine is not a chat).

4. **Delivery default.** `local` (write a deliverable file + in-app cue, §3.6b/c — works with no messaging layer) vs `channel` (push off-machine, requires the messaging hub) as the per-job default. Local is proposed; channel is opt-in once messaging lands.

5. **Consent default for autonomous runs.** `surface:'autonomous'` (default-deny ungranted mutation; pre-bless danger classes in `permissions.allow.json`, §3.5) is the only honest floor with no browser. Confirm there is **no** "cron approve mode" switch (the Hermes opt-out gen deliberately omits) — autonomous-default-deny is the proposed posture.

6. **Daily spend cap (§3.7 gap).** Build the cumulative `cron.spend.json` ledger now (an extra ≥1-commit step after Commit 4b), reusing the **already-frozen** `budget.threshold{scope:'day',usd,cap}` rung (`events.js:64`) for the exhaustion signal — or ship per-run caps only and accept the N×$cap unattended exposure for v1? A daily cap is strongly recommended *before* exposing minute/hour-granularity schedules.

7. **Whether a routine owns a workstream.** Routine-owns-a-workstream (one stream per routine, §3.9 — needs slice-2 wiring or a server-side per-job stream record) vs routine runs spill into General (collision-prone) vs no workstream link for v1. Routine-owns-a-stream is proposed; the working-now path is the server-side per-job record.

8. **Blueprint catalog scope.** The ~5 domain-agnostic seeds (briefing / station-recap / `[SILENT]`-monitor / topic-digest / custom-prompt-on-a-schedule, §3.10) vs a larger catalog vs custom-prompt-only (no catalog). The 5-seed catalog is proposed; the lifestyle blueprints are dropped.

9. **Run-history retention.** Last-run snapshot only (`lastRunAt`/`lastStatus`/`lastReason` on the record, the Hermes default) vs a bounded per-job output history (separate files, the Hermes `output/{id}/{ts}.md` shape, deferred to a run-log) vs full retention. Last-run-snapshot is proposed for v1; a deeper history rides the future ledger/run-log.

10. **Run-level wall-clock / idle kill (§3.3 hang-guard).** gen has **no** run-level wall-clock or idle-token deadline — the worst-case run is bounded only by `maxIters:16 × toolTimeoutMs:30000` ≈ 8 min (`index.js:31`, `sidecar/tools/registry.js:33`), and the §3.3 self-healing lease reclaims a zombie that exceeds `CRON_MAX_RUN_MS`. Is the lease ceiling enough, or do we add a per-job run-level deadline (a `CRON_MAX_RUN_MS` passed as the run's hard cap), given unattended is exactly where Hermes felt the need (`HERMES_CRON_TIMEOUT` default 600s, `scheduler.py:1767-1845`)? The lease is proposed as the minimum; an explicit per-run deadline is the stronger option.

11. **Transient-failure retry/backoff (§3.3).** On a fired run ending `agent.run.error{transient:true}` (the honest L3 signal at `loop.js:157`, currently unused by cron): schedule **one bounded retry** at `now + 60–120s` (capped at N attempts per occurrence, `retryCount` on the job, reset on the next clean fire) — or ship v1 with isolation only (a transient failure just waits the full period) and defer retry? Bounded-retry is recommended for unattended jobs (a 429 at 03:00 shouldn't lose the whole occurrence); if deferred it is a §3.3 deferred-with-a-note, not silently absent.
