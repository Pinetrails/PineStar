# PS-2026-069 — Operational-envelope Auditor experiment execution

## Authorization

Commander authorized exactly one execution of prepared plan `auditor-matched-local-operational-envelope-v1`, digest `a536bf0b3d6a1506548e83eb57a78536776552dc96a9ef93dd3cea1680b1a832`.

The plan compares active champion `operations-auditor.ollama-llama3.2-3b.v1` (`llama3.2:3b`) with isolated inactive challenger `operations-auditor.ollama-llama3.1-8b.v1` (`llama3.1:latest`) over objective-store, shared-report, and runtime-roster audits. It permits exactly six measured local Ollama runs, one attempt each, 240,000 ms first-response/header ceiling, zero pre-header or experiment retries, no recurrence/fallback, and maximum external cost `$0`.

## One-shot execution binding

- A distinct `PINE_STAR_AUTHORIZED_AUDITOR_OPERATIONAL_DIGEST` host-only gate accepts only the exact digest and PS-2026-069 authorization fields.
- Receipt `pine-star.auditor-operational-envelope-v1-receipt.json` is claimed with exclusive creation before model work. Existing consumed receipt paths are unchanged.
- All three inputs are snapshotted once before pre-warm or measurements, hash-checked, and reused across arms with deterministic identical instructions and condition hashes.
- Each arm releases the other resident model where practical, performs one tiny deterministic non-evidence pre-warm, executes its three measurements, and releases itself. Pre-warm/release activity is retained only in the execution receipt and creates no objective, run, or measurement evidence.
- Measured work uses normal `runOnce`, cancellation/E-stop registration, objective admission/lifecycle, run history, mechanically independent evidence, authoritative matched measurements, and existing evaluation/candidate gates.
- A provider-stream timeout below the independent 300-second idle threshold with no model result is recorded as a first-response timeout. It is a failed operational measurement, never infrastructure invalidity or rerun authority.
- Zero stored tokens retain explicit uncertainty unless provider processing is genuinely supported. No candidate is created unless the existing complete-evidence gate returns `challenger_recommended`; any candidate remains review-only and inactive.

## Pre-execution verification

Prepared executor, matched-measurement store, champion/challenger evaluation, configuration-candidate store, objective store, provider registry, provider timeouts, and loop recovery tests passed 344 focused assertions. Changed JavaScript syntax and diff checks passed. A canonical no-inference preflight must pass before the one-shot receipt is claimed.

## Results

Pending the authorized one-shot execution. No measured run has occurred under this plan at this point in Git history.
