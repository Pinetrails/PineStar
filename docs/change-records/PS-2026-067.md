# PS-2026-067 — Auditor replacement measurement

## Purpose

Implement the Commander decision to reconcile the consumed pre-inference attempt and execute one distinct, sealed six-run local Auditor replacement plan.

## Reconciliation

- Preserved objective IDs `objective:f9d75688-61ea-4244-bd9b-84b686f72efc` and `objective:698daf43-26b1-45b6-a683-677b6416c949`.
- Appended normal `failed` lifecycle settlements stating that `emit is not a function` occurred before inference.
- Preserved the original admission/lifecycle audit, consumed receipt, and absence of run, usage, token, cost, and measurement rows.

## Runner and safety

- Normalized the documented optional `runOnce` emitter at the reusable host boundary while retaining an explicit inert runner sink.
- Added an exact read-only preflight that validates digest, E-stop, champion binding, input readability/hash, and emitter callability while reporting zero model runs and zero measurements.
- Replacement plan: `auditor-matched-local-replacement-v1`.
- Digest: `cca3bd37e4d86e1247c60f26af34d76ca8c40c49cc32349d82355d729aee8301`.
- Its separate completed receipt links predecessor `auditor-matched-local-v1`, `PS-2026-066`, and the consumed receipt.
- No retry, recurrence, fallback, key rotation, task tools, auxiliary model work, model substitution, roster mutation, external action, or nonzero cost was permitted or observed.

## Verification

- Focused tests: 113 assertions before review; exact runner test later passed 22 assertions after receipt-provenance hardening.
- Changed-module syntax and diff checks passed.
- One security-sensitive diff review passed before canonical preflight/execution.
- Canonical no-inference preflight passed with three inputs, zero runs, and zero measurements.

## Measurement results

| Arm | Configuration | Outcomes | Runtime | Tokens | Cost |
| --- | --- | --- | ---: | ---: | ---: |
| Champion | `operations-auditor.ollama-llama3.2-3b.v1` | 0/3 completed; 3 provider-stream timeouts | 275,284 ms | UNKNOWN (recorded 0) | $0 |
| Challenger | `operations-auditor.ollama-llama3.1-8b.v1` | 0/3 completed; 3 provider-stream timeouts | 275,060 ms | UNKNOWN (recorded 0) | $0 |

All six normal run rows have `failureStage: provider_stream`, `failureCode: timeout`, empty tool/recovery/uncertain-mutation traces, zero measured cost, and no model response. Per-task input and instruction hashes match exactly across arms. Independent mechanical checks found valid JSON and zero specified structural issues in snapshots containing 10 objectives, 6 shared reports, and 1 roster agent; model/mechanical agreement is false because every model result is absent.

## Evaluation and retained lesson

- The complete matched-evidence marker was earned because all six settled objectives have authoritative same-task/same-condition measurement rows; it does not mean any run succeeded.
- Advisory verdict: `retain_champion`. Both configurations tied at zero completion, so there is no evidence that the challenger is better.
- Retained lesson: the present runtime envelope/provider stream timed out uniformly across both models and all tasks; this experiment measures shared timeout failure, not comparative Auditor quality.
- No review-only configuration candidate was created. Champion remains active; challenger remains inactive.
- Any timeout diagnosis, runtime-envelope change, or further experiment is a separate Commander decision.
