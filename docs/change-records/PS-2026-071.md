# PS-2026-071 — Auditor disagreement forensics and prospective v2 contract

## Evidence boundary

Commander retained active champion `operations-auditor.ollama-llama3.2-3b.v1` and permanently closed PS-2026-069. This change performs no inference, receipt access or mutation, evidence rewrite, routing/configuration change, timeout change, evaluation, or challenger activation.

## Findings

| Task | Model claim | Mechanical truth | Exact disagreement | Cause |
| --- | --- | --- | --- | --- |
| `shared-report-audit` | count `1`, zero issues, one invented `/evidence/...` reference | count `7`, zero issues, no references | Wrong count, extraneous reference, prose/Markdown around JSON | Model formatting and factual error; ambiguous v1 contract |
| `runtime-roster-audit` | count `4`, zero issues, four non-issue field references | count `1`, zero issues, no references | Wrong count, extraneous references, prose around JSON | Model formatting and factual error; ambiguous v1 contract |

Independent recomputation from the immutable snapshots reproduced both stored truths. The verifier correctly rejected non-JSON-only output. V1 nevertheless hid record-count semantics, issue predicates, markers, and the meaning of `evidenceRefs`, making it unsuitable for decision-quality comparison. Historical failures remain failures.

Supporting durable references are `run:2efd5a8e-0a91-4dd1-9231-bad7920f8a26` and `run:fe27b016-f715-4078-9e0b-6a7d208cc3cc`; immutable input hashes `2b5e770cddaa75410736bfa7f88d379069263e4b360cf3fd7869977ad2114f8d` and `26d731f85ff58b05c1482ab0da58b70c9c8422ec24b00279ef40c2c87cfebe79`; matching evidence files under `matched-measurements/auditor-matched-local-operational-envelope-v1/`; exact outputs in `transcript-history-v2/segment-000001.jsonl`; run provenance in `runs.jsonl`; and the historical verifier in `sidecar/prepared-auditor-experiment.js`.

## Prospective correction and recommendation

Added non-executable `auditor-json-audit-v2` with distinct `*-v2` identities, explicit input/count/issue rules, an exact raw-JSON schema, and `issueCount === evidenceRefs.length`. Synthetic tests cover deterministic truth and malformed output. V1 and historical artifacts are unchanged.

A new experiment is justified only after separate authorization: three v2 tasks, champion and inactive challenger, three runs per task per arm (18 runs), one attempt each, local Ollama, `$0`, no retries/fallback/recurrence/tools/mutations, existing 240-second first-response ceiling, 90-minute wall envelope, paired immutable snapshots, exact hashes, raw output retention, independent truth recomputation, and complete provenance. Rollback closes the new receipt while retaining the champion and inactive challenger. No receipt or executable plan is created here.
