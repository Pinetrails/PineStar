# PS-2026-072 — Prepared repeated Auditor v2 matched-experiment plan

## Authorization and boundary

Commander accepted PS-2026-071 and authorized plan/digest creation only. The resulting module has no execution function, receipt creation/claim, host trigger, route, API, scheduler, objective/run/measurement writer, model call, or pre-warm. It changes no champion, challenger state, routing, configuration, timeout, evidence gate, historical artifact, or external-cost authority.

## Immutable plan

- Plan ID: `auditor-matched-local-v2-repeated-v1`
- Canonical SHA-256: `8fd60778992aa9c478284d8c6bf338266579eddb9759cfb60e49ad4bc34fecd3`
- Role: `operations.auditor`
- Champion: `operations-auditor.ollama-llama3.2-3b.v1` / local Ollama `llama3.2:3b`
- Challenger: inactive `operations-auditor.ollama-llama3.1-8b.v1` / local Ollama `llama3.1:latest`
- Contract: `auditor-json-audit-v2`
- Tasks: `objective-store-audit-v2`, `shared-report-audit-v2`, `runtime-roster-audit-v2`
- Structure: three repetitions of each task in each arm; 18 maximum measured slots and nine matched pairs
- Inputs: read-only reuse of the three named immutable PS-2026-069 snapshots, with fixed input hashes and newly generated v2 instruction hashes

Each slot has a deterministic identity and binds arm, task, repetition, input hash, and instruction hash. A valid repetition requires exactly-once slot execution, matching identity, normal run provenance, exact raw-output retention, independent v2 truth recomputation, truthful settlement, and complete cost/usage/retry/tool/mutation/safety/timing uncertainty. Timeout and incorrect output are failed measurements; UNKNOWN is retained rather than inferred.

## Operational and time envelope

The planned execution boundary is loopback Ollama, 240,000 ms first-response/header ceiling, zero pre-header or automatic retries, one attempt per slot, no recurrence/fallback/tools/routing or configuration mutation, and maximum external cost `$0`. The maximum planned wall envelope is 90 minutes; this is a cap, not an expected duration. Eighteen sequential slots have a strict response-ceiling total of 72 minutes, leaving 18 minutes for bounded host settlement overhead.

The practical estimate is 35–65 minutes. Its limited basis is the two completed PS-2026-069 champion responses at 140,526 ms and 178,462 ms; the challenger has no completed operational-envelope latency, so the range remains uncertain and ceiling-bound outcomes are plausible.

## Repetition assessment

Three repetitions per task materially improve evidence quality by exposing within-task stochastic formatting, correctness, and latency variation and by allowing a two-of-three reliability pattern instead of letting one sample decide an arm/task result. A six-run design has one observation per arm/task and no repeatability evidence. A 12-run design has two observations per arm/task, detects instability, but a split result cannot distinguish an isolated failure from the dominant behavior. Both smaller designs reduce runtime, but 6 materially and 12 moderately reduce confidence.

Eighteen runs remain recommended and proportionate for a configuration-quality decision: the work is local and `$0`, the contract defect is corrected, and the decision could affect a production role, while activation still remains separately protected. A 12-run alternative is reasonable only if Commander prioritizes time over majority-pattern evidence; it would use two repetitions per task per arm, 12 slots, a 48-minute strict measured ceiling, an estimated 24–44 minutes, and a proposed 65-minute wall cap. No execution scope is authorized here.
