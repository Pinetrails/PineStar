# PS-2026-065 — Sealed Auditor matched-measurement runner

## Purpose

Execute only the Commander-authorized six-run local Auditor plan without exposing inactive configurations to normal routing or a generic execution API.

## Changes

- Compiled the exact role, configurations, models, three tasks, local provider, zero cost, no-retry, and no-recurrence terms into a canonical SHA-256 plan digest.
- Added a private environment-triggered host path requiring that exact digest and a missing durable prior-attempt receipt.
- Reused normal objective creation/admission/lifecycle, `runOnce`, E-stop prechecks, active-run cancellation maps, run history, and private matched-measurement authority.
- Snapshots each source once per task and supplies identical bytes and instruction hashes to both arms.
- Deterministically recalculates requested properties, persists model/mechanical results and agreement, and refuses fluent but mechanically incorrect output as completion.
- Keeps the challenger as a per-run in-memory configuration derived from the champion; it never enters the roster or normal role routing.

## Verification

- Exact-plan runner: PASS (14 assertions), covering digest/config/provider/cost/retry/recurrence refusal, six-run bound, arm isolation, parity, mechanical agreement, and E-stop refusal.
- Changed-module syntax and diff checks passed.

## Safety

- No HTTP route, generic objective field, arbitrary model input, scheduling path, fallback, paid provider, or production roster mutation was added.
- A durable receipt makes the authorized plan one-shot; failures are recorded and never retried automatically.
- Every authoritative measurement requires an existing normal run row with exact Ollama/configuration identity and zero measured cost.
- Challenger activation remains unauthorized regardless of evaluation outcome.
