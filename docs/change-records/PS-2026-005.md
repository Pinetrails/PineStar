# PS-2026-005 — Reports Surface and Control Status

**Date:** 2026-08-26
**Status:** Complete
**Type:** Phase 3 observability and control synchronization
**Git commit:** The commit containing this record

## Purpose

Make the shared-report boundary useful to the Commander and expose one machine-readable runtime control contract.

## Implementation

- Added a REPORTS entry to the existing WORK menu and window framework.
- Added a read-only Reports view with concise Completed, Exceptions, Decisions, Next Actions, and Sources sections.
- Escaped every report field before rendering and omitted empty sections.
- Added authenticated `GET /api/control/status` with versioned schema, private/shared store health, durable report count, latest report time, external-sync state, and the `$0` spending-authority default.
- The UI states that external synchronization is off; no external Obsidian vault or service is read or modified.
- Synchronized the generated website application mirror.

## Verification

- `reports-ui.test.js`: **PASS** (4 assertions).
- `apiauth.test.js`: **PASS** (54 assertions).
- `routes-coverage.http.test.js`: **PASS** (98 assertions).
- `website-app-sync.test.js`: **PASS** (8 assertions).
- Edited JavaScript syntax and whitespace checks: **PASS**.

## Next work

Phase 3 can now add deliberate export/adaptation contracts for human-readable reports and richer project-control projections without exposing internal memory. External Obsidian writes remain out of scope until mapping, privacy, conflict, and user-approved write scope exist.
